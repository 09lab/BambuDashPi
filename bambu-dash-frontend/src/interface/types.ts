export interface PrinterConfigData {
    printerName: string
    printerIp: string
    printerSerial: string
    printerCode: string
}

export interface PrinterInfo {
  name: string;
  image: string;
}